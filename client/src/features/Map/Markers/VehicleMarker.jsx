import React from 'react';
import { Icon } from '@iconify/react';

/**
 * DESC: Vehicle Marker component for the map
 * PROPS: none
 */
const VehicleMarker = () => {
    return (
        <Icon icon="ant-design:car-filled" className="location-icon" /> 
    );
};

export default VehicleMarker;
